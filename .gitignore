# Dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local
repos.txt

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo
*~

# Temp files
/tmp/
*.tmp

# Private credentials (if stored in separate files)
credentials.json
secrets.json
config.local.js

# Personal daily notes (copy from standup-notes.example.txt)
standup-notes.txt

# Latest run errors only (overwritten each run; empty when the last run succeeded)
standup-errors.txt

# Monthly Cliq worker (see MONTHLY-CLIQ.md)
monthly-cliq.config.json
.last-monthly-cliq
monthly-cliq-errors.txt

# Weekly standup worker (Tuesdays; see docs/WEEKLY-STANDUP.md)
weekly-standup-errors.txt
